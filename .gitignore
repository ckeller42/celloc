# Build output (anchored to repo root / dist so they don't match cmd/geolocd, etc.)
/dist/
/bin/
/geolocd
/geoinflux
/celloc
*.ipk

# Test / coverage
*.out
cover.out
coverage.*

# Editor / OS
.DS_Store
*.swp
